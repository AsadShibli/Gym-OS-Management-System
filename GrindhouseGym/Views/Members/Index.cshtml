@model IEnumerable<Member>

@{
    ViewData["Title"] = "Member Directory";
}

<div class="container mt-4">
    <div class="row align-items-center mb-4">
        <div class="col-md-6">
            <h2 class="fw-bold text-primary">Member Directory</h2>
            <p class="text-muted mb-0">Manage active gym memberships and track expiration.</p>
        </div>
        <div class="col-md-6">
            <div class="d-flex gap-2 justify-content-md-end">
                <div class="input-group" style="max-width: 300px;">
                    <span class="input-group-text bg-white border-end-0">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search text-muted" viewBox="0 0 16 16">
                            <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z" />
                        </svg>
                    </span>
                    <input type="text" id="searchBox" onkeyup="searchTable()" class="form-control border-start-0" placeholder="Search members...">
                </div>

                <a asp-action="Create" class="btn btn-primary shadow-sm fw-bold">
                    <i class="bi bi-person-plus-fill me-1"></i> New Member
                </a>
            </div>
        </div>
    </div>

    <div class="card shadow border-0">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0 align-middle" id="membersTable">
                <thead class="bg-light">
                    <tr>
                        <th class="ps-4 py-3">Full Name</th>
                        <th class="py-3">Email Address</th>
                        <th class="py-3">Join Date</th>
                        <th class="py-3">Duration</th>
                        <th class="py-3">Status</th>
                        <th class="py-3">Current Plan</th>
                        <th class="text-end pe-4">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        // === LOGIC: Calculate Status for each row ===
                        var endDate = item.JoinDate.AddMonths(item.Duration);
                        var remainingDays = (endDate - DateTime.Now).Days;
                        bool isActive = remainingDays > 0;
                        // ============================================

                        <tr>
                            <td class="ps-4 fw-bold text-dark name-cell">
                                @Html.DisplayFor(modelItem => item.FullName)
                            </td>
                            <td class="text-muted">
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                <span class="text-secondary small">
                                    <i class="bi bi-calendar3 me-1"></i>
                                    @item.JoinDate.ToString("MMM dd, yyyy")
                                </span>
                            </td>

                            <td>
                                <span class="fw-bold text-dark">@item.Duration</span> <span class="text-muted small">Mo</span>
                            </td>

                            <td>
                                @if (isActive)
                                {
                                    <span class="badge bg-success bg-opacity-10 text-success px-3 py-1 rounded-pill border border-success border-opacity-25">
                                        Active
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger bg-opacity-10 text-danger px-3 py-1 rounded-pill border border-danger border-opacity-25">
                                        Expired
                                    </span>
                                }
                            </td>

                            <td>
                                <span class="badge bg-primary text-white px-3 py-1 rounded-pill">
                                    @Html.DisplayFor(modelItem => item.Plan.Name)
                                </span>
                            </td>
                            <td class="text-end pe-4">
                                <div class="btn-group">
                                    <a asp-action="Edit" asp-route-id="@item.MemberId" class="btn btn-sm btn-outline-secondary" title="Edit">
                                        Edit
                                    </a>
                                    <a asp-action="Details" asp-route-id="@item.MemberId" class="btn btn-sm btn-outline-primary" title="View">
                                        Details
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.MemberId" class="btn btn-sm btn-outline-danger" title="Remove">
                                        Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>
    function searchTable() {
        var input = document.getElementById("searchBox");
        var filter = input.value.toUpperCase();
        var table = document.getElementById("membersTable");
        var tr = table.getElementsByTagName("tr");

        for (var i = 1; i < tr.length; i++) {
            var td = tr[i].getElementsByTagName("td")[0]; // Column 0 is Name
            if (td) {
                var txtValue = td.textContent || td.innerText;
                if (txtValue.toUpperCase().indexOf(filter) > -1) {
                    tr[i].style.display = "";
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
    }
</script>